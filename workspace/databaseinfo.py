import mysql.connector

class Sqlconnector:
    db = mysql.connector.connect(
    host = #removed (app will not run)
    port = #removed
    user = #removed
    password = #removed
    database = #removed
    )

